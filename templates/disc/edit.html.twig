{% extends 'base.html.twig' %}

{% block title %}Modifier le disque
{% endblock %}

{% block body %}
	<div class="container">
		<div class="row mt-5">
			<h1 class="d-block  d-flex justify-content-center bg-primary mb-5">Modifier le disque</h1>
		</div>
		<div class="row g-2">


			{{ include('disc/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

			<a href="{{ path('app_disc_index') }}">Retour a la liste des disques</a>

			{{ include('disc/_delete_form.html.twig') }}
		</div>
	</div>
{% endblock %}
